/* Write your PL/SQL query statement below */
Select p.firstName, p.lastName, a.city, a.state
from Person p left join Address a /*left join so that we get all the records from Person table*/
on p.personId = a.personId;
